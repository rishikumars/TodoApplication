import { TargetDateValidatorDirective } from './target-date-validator.directive';

describe('TargetDateValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TargetDateValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
